/**
 * Phase F target. Phase A scaffold holds the workspace so the root
 * `npm install` succeeds. `npm run dev:desktop` etc. are wired up when the
 * mobile app is implemented.
 */
export const PHASE_F_PENDING = true